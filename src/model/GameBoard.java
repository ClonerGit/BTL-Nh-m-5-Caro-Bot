package model;


public class GameBoard {
    public void placeMove(int row, int col, char symbol) {
        System.out.println("Đánh tại: " + row + ", " + col + " bởi: " + symbol);
        // TODO: Ghi vào ma trận bàn cờ nếu có
    }
}
